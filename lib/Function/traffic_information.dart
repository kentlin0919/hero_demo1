import 'package:flutter/material.dart';

class traffic_information extends StatelessWidget {
  traffic_information({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: Center(
      child: Text('pp'),
    ));
  }
}
